
@{
ViewData["Title"] = "Products Page";
}

@model ProductRecsViewModel

@* Still need to create button that takes product and adds it too cart if the user is logged in. *@
@* If not, send to login page *@
@* Worry about this last - Configure routing at that time (not in home controller yet) *@

<div class="container product-details">
        <div class="row">
            <div class="col-md-6 product-image">
                @if (!string.IsNullOrEmpty(Model.Product.img_Link))
                {
                    <img src="@Model.Product.img_Link" alt="Image of @Model.Product.ProductName">
                }
            </div>
            <div class="col-md-6">
                <h2 class="product-title">@Model.Product.ProductName</h2>
                <p class="product-price">Price: $@Model.Product.Price</p>
                <p class="product-info">Category: @Model.Product.Category</p>
                <p class="product-info">Color: @Model.Product.primary_Color</p>
                <p class="product-info indent">Color: @Model.Product.Description</p>
                @if (Model.Recommendations.Any())
                {
                    <div>
                        <h3 class="product-title">Aurora's Recommendataions For You</h3>
                        @foreach (var rec in Model.Recommendations)
                        {
                            <div>
                                <h4>@rec.ProductName</h4>
                                <!-- Assuming there is an image link property -->
                                <img src="@rec.img_Link" alt="Image of @rec.ProductName" style="max-width:100%; height:auto;">
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>

<html>


